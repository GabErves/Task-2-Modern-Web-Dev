const add = (n1, n2) => parseInt(n1, 10)+parseInt(n2,10);


export {add};


const multiply = (n1, n2) => parseInt(n1,10)*parseInt(n2, 10);

export {multiply};


const subtract = (n1, n2) => parseInt(n1,10)-parseInt(n2,10);

export {subtract};

const divide = (n1, n2) => parseInt(n1,10)/parseInt(n2,10);

export {divide};

const modulo = (n1, n2) => parseInt(n1,10)%parseInt(n2,10);

export {modulo};


